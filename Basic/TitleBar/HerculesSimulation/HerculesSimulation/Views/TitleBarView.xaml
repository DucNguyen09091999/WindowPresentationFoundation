<UserControl x:Class="HerculesSimulation.Views.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HerculesSimulation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="640">
    
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

            <Button x:Name="btnMinimize" Content="-" Width="40" Height="40" 
                    BorderBrush="Transparent" Background="Transparent" 
                    Foreground="White"
                    WindowChrome.IsHitTestVisibleInChrome="True"
                    Command="{Binding MinimizeCommand}"/>

            <Button x:Name="btnMaximize" Content="□" Width="40" Height="40" 
                    BorderBrush="Transparent" Background="Transparent" 
                    Foreground="White"
                    WindowChrome.IsHitTestVisibleInChrome="True"
                    Command="{Binding MaximizeCommand}"/>

            <Button x:Name="btnClose" Content="X" Width="40" Height="40" 
                        BorderBrush="Transparent" Background="Transparent" 
                        Foreground="White"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        Command="{Binding CloseCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </StackPanel>
</UserControl>
